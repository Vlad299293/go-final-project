Был создан планировщик задач с возможностью добавления, удаления, редактирования, повторения. при отметке "выполнено" задача автоматически удаляется. добавление, удаление, редактирование, повторение реализованы в файлах addtask, deletetask, edittask, nextdate - соответсвенно. 
обработаны все возможные ошибки с помощью errors.

Выполненные задания со звёздочкой:

1)Определение порта через переменную окружения TODO_PORT (по умолчанию 7540, если добавить через переменную, будет использовать ее)
2)Определение пути к файлу БД через переменную окружения TODO_DBFILE
3)Полная поддержка правил повторения w (дни недели) и m (дни/месяцы, включая -1 и -2)
4)Поиск задач по подстроке и по дате (GET /api/tasks?search=...) (но он чувствителен к регистру)
5)Аутентификация по паролю (JWT), переменная окружения TODO_PASSWORD

Докер не делал

Запуск:
go mod tidy
go run main.go / (если с паролем) $env:TODO_PASSWORD="12345"; go run main.go

http://localhost:7540/
